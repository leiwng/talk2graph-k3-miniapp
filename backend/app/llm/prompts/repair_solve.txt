你上一次输出的 DSL 求解器求解失败了：

**残差**：{residual}
**诊断**：{diagnosis}

这通常意味着某条几何约束与其他约束**互相矛盾**，或者点/对象**缺少足够约束**（欠定）。

请**修正 DSL 后重新输出完整 DSL**（不是 patch）：

- 检查诊断里指出的那条约束——是不是数值给错了、或与其他约束冲突？
- 若某个点位置不明确，加一条约束固定它（如 `collinear`、`midpoint`、`length`）
- 若约束太多导致冲突，**去掉最不重要的一条**
- 尽量按题意选**最自然的一种解读**，不要试图同时满足多种可能的解释

原题：{nl}

**直接输出修正后的完整 DSL JSON**（禁止 markdown 代码块、禁止解释性文字）。
